# class Model:
#     def __init__(self, model_name, method, hyperparams, scores):
#         self.model_name = model_name
#         self.method = method
#         self.hyperparams = hyperparams
#         self.scores = scores

class Classifier:
    def __init__(self, model_name, method, hyperparams, scores):
        self.model_name = model_name
        self.method = method
        self.hyperparams = hyperparams
        self.scores = scores
